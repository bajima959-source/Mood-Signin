## Packages
framer-motion | Page transitions and UI animations
recharts | Visualizing mood trends over time
date-fns | Formatting dates for logs and charts
clsx | Utility for conditional class names (usually standard but good to be safe)
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Outfit'", "sans-serif"],
  body: ["'DM Sans'", "sans-serif"],
}
